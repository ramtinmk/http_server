#ifndef TEST_SERVER_H
#define TEST_SERVER_H

// Runs the server test suite.
// 'filter' is an optional string to run only specific tests (e.g., "gzip", "404").
void run_server_tests();

#endif // TEST_SERVER_H